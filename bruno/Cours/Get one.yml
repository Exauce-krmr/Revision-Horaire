info:
  name: Get one
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE_URL}}/{{ENDPOINT}}/{{id}}"
  auth: inherit

runtime:
  variables:
    - name: id
      value:
        type: number
        data: "1"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
